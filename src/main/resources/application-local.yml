server:
  port: 8082

spring:
  config:
    activate:
      on-profile: local
  datasource:
   driver-class-name: com.mysql.cj.jdbc.Driver
#   db포트 3307
   url: jdbc:mysql://localhost:3307/oauthdb?useSSL=false
   username: root
   password: test1234

  jpa:
    database: mysql
    database-platform: org.hibernate.dialect.MySQL8Dialect
    generate-ddl: true
    hibernate:
      ddl-auto: create
    show-sql: true

logging:
  level:
    root: info